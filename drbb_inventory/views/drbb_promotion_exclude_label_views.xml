<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="drbb_promotion_exclude_label_view_form" model="ir.ui.view">
        <field name="name">drbb.promotion.exclude.label.view.form</field>
        <field name="model">drbb.promotion.exclude.label</field>
        <field name="arch" type="xml">
            <form string="Promotion Exclude Labels">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name"/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <record id="drbb_promotion_exclude_label_view_list" model="ir.ui.view">
        <field name="name">drbb.promotion.exclude.label.view.list</field>
        <field name="model">drbb.promotion.exclude.label</field>
        <field name="arch" type="xml">
            <list string="Promotion Exclude Labels">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="drbb_promotion_exclude_label_view_search" model="ir.ui.view">
        <field name="name">drbb.promotion.exclude.label.search</field>
        <field name="model">drbb.promotion.exclude.label</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
            </search>
        </field>
    </record>

    <record id="action_drbb_promotion_exclude_label" model="ir.actions.act_window">
        <field name="name">Promotion Exclude Labels</field>
        <field name="res_model">drbb.promotion.exclude.label</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
